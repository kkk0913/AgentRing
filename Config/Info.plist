<?xml version="1.0" encoding="UTF-8"?>
<!--
    Static Info.plist for the Agent Ring app target.

    Switched from Xcode's auto-generated Info.plist (GENERATE_INFOPLIST_FILE = YES)
    to this static file so custom keys are not silently dropped by the
    INFOPLIST_KEY_* code path (only Apple-known keys survive there).
    Adding new keys → edit here.

    Build settings still interpolate via $(VARIABLE_NAME), so version bumps in
    project.pbxproj's MARKETING_VERSION / CURRENT_PROJECT_VERSION flow through.

    Sparkle verifies updates using our EdDSA key; Apple Developer ID is not required.
    SPARKLE_PUBLIC_ED_KEY is supplied by the release build from its signing key.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIconFile</key>
    <string>AppIcon</string>
    <key>CFBundleIconName</key>
    <string>AppIcon</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>Agent Ring</string>
    <key>CFBundleDisplayName</key>
    <string>Agent Ring</string>
    <key>CFBundlePackageType</key>
    <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>LSApplicationCategoryType</key>
    <string>public.app-category.utilities</string>
    <key>LSMinimumSystemVersion</key>
    <string>$(MACOSX_DEPLOYMENT_TARGET)</string>
    <key>LSUIElement</key>
    <true/>
    <key>NSHumanReadableCopyright</key>
    <string></string>
    <key>NSBluetoothAlwaysUsageDescription</key>
    <string>Agent Ring 需要蓝牙权限以向 Android 桌面副屏推送用量额度数据</string>
    <key>SUFeedURL</key>
    <string>https://github.com/kkk0913/AgentRing/releases/latest/download/appcast.xml</string>
    <key>SUPublicEDKey</key>
    <string>$(SPARKLE_PUBLIC_ED_KEY)</string>
    <key>SUEnableInstallerLauncherService</key>
    <true/>
    <key>SUEnableDownloaderService</key>
    <false/>
    <key>SUVerifyUpdateBeforeExtraction</key>
    <true/>
    <key>SURequireSignedFeed</key>
    <true/>
    <key>SUEnableAutomaticChecks</key>
    <true/>
    <key>SUScheduledCheckInterval</key>
    <integer>3600</integer>
    <key>SUAutomaticallyUpdate</key>
    <false/>
    <key>SUAllowsAutomaticUpdates</key>
    <false/>
    <key>SUEnableSystemProfiling</key>
    <false/>
</dict>
</plist>
